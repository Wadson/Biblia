<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Biblia.Presentation.Components.PageHeaderView"
             x:Name="Root"
             BackgroundColor="{StaticResource BtHeader}"
             HorizontalOptions="Fill"
             SemanticProperties.Description="Cabeçalho da página">
    <Grid HeightRequest="56" ColumnDefinitions="*,Auto" ColumnSpacing="8">
        <Label Text="{Binding Title, Source={x:Reference Root}}"
               TextColor="White"
               FontSize="20"
               FontAttributes="Bold"
               LineBreakMode="TailTruncation"
               MaxLines="1"
               VerticalTextAlignment="Center"
               SemanticProperties.HeadingLevel="Level1" />
        <Button x:Name="BackButton"
                Grid.Column="1"
                Text="←  Voltar"
                IsVisible="{Binding ShowBackButton, Source={x:Reference Root}}"
                Command="{Binding InternalBackCommand, Source={x:Reference Root}}"
                MinimumHeightRequest="44"
                HeightRequest="44"
                MinimumWidthRequest="96"
                Padding="12,0"
                BackgroundColor="Transparent"
                BorderColor="Transparent"
                TextColor="{StaticResource BtGoldSoft}"
                FontSize="15"
                FontAttributes="Bold"
                CornerRadius="10"
                SemanticProperties.Description="Voltar para a tela anterior"
                SemanticProperties.Hint="Retorna preservando o estado da tela anterior">
            <VisualStateManager.VisualStateGroups>
                <VisualStateGroupList>
                    <VisualStateGroup x:Name="CommonStates">
                        <VisualState x:Name="Normal" />
                        <VisualState x:Name="PointerOver">
                            <VisualState.Setters><Setter Property="BackgroundColor" Value="#182D42" /></VisualState.Setters>
                        </VisualState>
                        <VisualState x:Name="Pressed">
                            <VisualState.Setters><Setter Property="BackgroundColor" Value="#243B52" /></VisualState.Setters>
                        </VisualState>
                        <VisualState x:Name="Disabled">
                            <VisualState.Setters><Setter Property="Opacity" Value="0.55" /></VisualState.Setters>
                        </VisualState>
                    </VisualStateGroup>
                </VisualStateGroupList>
            </VisualStateManager.VisualStateGroups>
        </Button>
    </Grid>
</ContentView>
